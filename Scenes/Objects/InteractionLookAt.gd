extends Sprite3D

func _process(delta: float) -> void:
	#if self.visible:
		#var camera = get_tree().get_first_node_in_group("camera")
		#var camera_dir = -camera.transform.basis.z
		#var up_vector = Vector3.UP
		#transform.basis = Basis(up_vector.cross(camera_dir), up_vector, camera_dir).orthonormalized()
	pass
